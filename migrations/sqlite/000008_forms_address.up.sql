-- No-op on SQLite: the question_type CHECK set was declared at its final value
-- in 000001 (SQLite cannot ALTER a CHECK constraint), so this step of the
-- PostgreSQL history has nothing left to do.
SELECT 1;
