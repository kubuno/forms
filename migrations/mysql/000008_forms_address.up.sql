-- No-op on MySQL: the question_type CHECK set was declared at its final value
-- in 000001 (a MySQL CHECK is not evolved incrementally here), so this step of
-- the PostgreSQL history has nothing left to do.
DO 0;
